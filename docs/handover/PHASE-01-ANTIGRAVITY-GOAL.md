# DEPRECATED — PHASE 01 ANTIGRAVITY GOAL

> **KHÔNG ĐƯỢC CHẠY.** Goal Antigravity triển khai toàn bộ Phase 01 đã bị hủy sau một phiên thực thi gián đoạn và không còn là nguồn chỉ dẫn hợp lệ.

## Thay thế

Quy trình phân công Codex/Antigravity hiện hành nằm trong `AGENTS.md`. Handover dành cho bước triển khai kế tiếp nằm tại `docs/handover/PHASE-01-CODEX-HANDOVER.md`.

Mọi task mới phải:

- bắt đầu bằng dòng công cụ thực thi bắt buộc;
- dùng một branch riêng;
- tuân theo addendum Phương án B v1.3 và ADR hiện hành;
- giới hạn Antigravity ở task hẹp được cho phép rõ ràng;
- giao schema/migration, auth/authorization, CI/CD, security và thay đổi phức tạp cho Codex;
- trải qua kiểm tra, commit/push và review GitHub độc lập trước mọi quyết định merge/deploy.

Nội dung goal cũ có thể truy xuất từ lịch sử Git tại commit `edb9733` chỉ để điều tra; không sao chép hoặc thực thi lại.
