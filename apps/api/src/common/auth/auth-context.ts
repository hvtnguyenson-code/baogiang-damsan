/**
 * auth-context.ts
 * Authentication context interfaces for the API layer.
 *
 * Phase 00: Type definitions only.
 * Authentication implementation comes in Phase 01.
 */
export type {
  AuthContext,
  ScopedCapability,
  CapabilityKey,
  CapabilityScope,
} from '@baogiang/contracts';
