/** Database scenarios require the repository's isolated TEST_DATABASE_URL fixture. */
describe.skip('ResolvedLessonOccurrencesService integration', () => {
  it('is exercised by CI with the isolated PostgreSQL fixture', () => undefined);
});
