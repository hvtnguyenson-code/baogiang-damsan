import { AuditResult, PrismaClient, ReportingStatementLifecycleState as State, UserStatus } from '@prisma/client';
import { AuditService } from '../../src/audit/audit.service';
import { ReportingStatementsService } from '../../src/reporting-statements/reporting-statements.service';
import { ReportingStatementRepository } from '../../src/reporting-statement-internal/reporting-statement.repository';
import { freezeReportingStatementSnapshot } from '../../src/reporting-statement-internal/reporting-statement-canonicalizer';
import { integration, testDatabaseUrl } from '../helpers/phase01-test-harness';
const asOf = new Date('2026-08-24T01:02:03.004Z');
const personalProjection = (subjectId: string, userId: string, yearId: string) => ({ profile: 'PERSONAL_TEACHING_REPORTING_PROJECTION_V1', scope: { academicYearId: yearId, targetUserId: userId, fromCivilDate: '2026-08-01', toCivilDate: '2026-08-31', asOfInstant: asOf }, responsibilityState: 'RESPONSIBILITY_PRESENT', status: 'PASS', counts: { distributedElapsedCount: 1, completedCount: 1, openDebtCount: 0, lateCount: 0, unconfirmedGapCount: 0 }, responsibilityManifest: [{ teachingAssignmentId: 'a', schoolClassId: 'c', subjectId, validFrom: '2026-08-01', validUntil: null }], sections: [], findings: [], evaluatedAt: asOf.toISOString() });
const frozen = (subjectId: string, userId: string, yearId: string) => freezeReportingStatementSnapshot({ statementProfile: 'PERSONAL_V1', submitterUserId: userId, asOfInstant: asOf, projection: personalProjection(subjectId, userId, yearId) as never });
integration('Reporting Statements control-plane PostgreSQL', () => { const repository = new ReportingStatementRepository(); const projection = { resolveInTransaction: jest.fn() }; let prisma: PrismaClient; let submitter = ''; let approver = ''; let subject = ''; let year = ''; let service: ReportingStatementsService;
  const request = (id: string) => ({ auth: { user: { id, mustChangePassword: false } }, headers: {} }) as never;
  const cleanupReportingStatements = async () => { await prisma.reportingStatementHistory.deleteMany(); await prisma.reportingStatementCommand.deleteMany(); await prisma.reportingStatementRevisionSubject.deleteMany(); await prisma.reportingStatementRevisionState.deleteMany(); await prisma.reportingStatementRevision.updateMany({ data: { predecessorRevisionId: null, supersedesRevisionId: null } }); await prisma.reportingStatementRevision.deleteMany(); await prisma.reportingStatementSeries.deleteMany(); };
  const cleanupFixtureParents = async () => { const userIds = [submitter, approver].filter(Boolean); if (userIds.length) await prisma.auditEvent.deleteMany({ where: { actorUserId: { in: userIds } } }); if (year) { await prisma.teachingAssignment.deleteMany({ where: { academicYearId: year } }); await prisma.schoolClass.deleteMany({ where: { academicYearId: year } }); } if (subject) await prisma.subject.delete({ where: { id: subject } }); if (year) await prisma.academicYear.delete({ where: { id: year } }); if (userIds.length) await prisma.user.deleteMany({ where: { id: { in: userIds } } }); submitter = ''; approver = ''; subject = ''; year = ''; };
  const seed = async (input: { predecessorRevisionId?: string; supersedesRevisionId?: string } = {}) => prisma.$transaction(tx => repository.persistSubmittedRevision(tx, { series: { statementProfile: 'PERSONAL_V1', submitterUserId: submitter, academicYearId: year, fromCivilDate: new Date('2026-08-01'), toCivilDate: new Date('2026-08-31') }, frozen: frozen(subject, submitter, year), revision: input, lifecycleToken: crypto.randomUUID(), command: { actorUserId: submitter, requestKey: crypto.randomUUID(), requestFingerprint: crypto.randomUUID() }, history: { actorUserId: submitter } }));
  beforeAll(async () => { prisma = new PrismaClient({ datasources: { db: { url: testDatabaseUrl } } }); const auth = { evaluate: jest.fn().mockResolvedValue({ allowed: true }) }; service = new ReportingStatementsService(prisma as never, repository, projection as never, auth as never, new AuditService(prisma as never), { now: jest.fn(() => asOf) }); });
  beforeEach(async () => { projection.resolveInTransaction.mockReset(); await cleanupReportingStatements(); await prisma.auditEvent.deleteMany(); submitter = (await prisma.user.create({ data: { username: `s-${crypto.randomUUID()}`, passwordHash: 'x', status: UserStatus.ACTIVE, mustChangePassword: false } })).id; approver = (await prisma.user.create({ data: { username: `a-${crypto.randomUUID()}`, passwordHash: 'x', status: UserStatus.ACTIVE, mustChangePassword: false } })).id; year = (await prisma.academicYear.create({ data: { code: `Y${crypto.randomUUID().slice(0, 6).toUpperCase()}`, name: 'Y' } })).id; subject = (await prisma.subject.create({ data: { code: `S${crypto.randomUUID().slice(0, 6).toUpperCase()}`, name: 'S' } })).id; });
  afterEach(async () => { await cleanupReportingStatements(); await cleanupFixtureParents(); });
  afterAll(async () => { await cleanupReportingStatements(); await cleanupFixtureParents(); await prisma.$disconnect(); });
  it('rejects seeded submitted revision with command, history and audit', async () => { const seeded = await seed(); const before = seeded.state.lifecycleToken; await service.decide(seeded.revision.id, { expectedLifecycleToken: before, requestKey: 'reject' }, request(approver), 'REJECT'); const state = await prisma.reportingStatementRevisionState.findUnique({ where: { revisionId: seeded.revision.id } }); expect(state?.lifecycleState).toBe(State.REJECTED); expect(state?.lifecycleToken).not.toBe(before); expect(await prisma.reportingStatementCommand.count({ where: { commandType: 'REJECT', requestKey: 'reject' } })).toBe(1); expect(await prisma.reportingStatementHistory.count({ where: { revisionId: seeded.revision.id, eventType: 'REJECTED' } })).toBe(1); expect(await prisma.auditEvent.count({ where: { action: 'REPORTING_STATEMENT_REJECTED', result: AuditResult.SUCCESS } })).toBe(1); });
  it('rejects stale token without decision artifacts', async () => { const seeded = await seed(); await expect(service.decide(seeded.revision.id, { expectedLifecycleToken: crypto.randomUUID(), requestKey: 'stale' }, request(approver), 'REJECT')).rejects.toThrow(); const state = await prisma.reportingStatementRevisionState.findUnique({ where: { revisionId: seeded.revision.id } }); expect(state?.lifecycleToken).toBe(seeded.state.lifecycleToken); expect(await prisma.reportingStatementCommand.count({ where: { commandType: 'REJECT' } })).toBe(0); });
  it.each(['APPROVE', 'REJECT'] as const)('denies self %s without accepted decision artifacts', async command => {
    const seeded = await seed();
    const eventType = command === 'APPROVE' ? 'APPROVED' : 'REJECTED';
    await expect(service.decide(seeded.revision.id, { expectedLifecycleToken: seeded.state.lifecycleToken, requestKey: `self-${command}` }, request(submitter), command)).rejects.toThrow();
    expect(await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: seeded.revision.id } })).toMatchObject({ lifecycleState: State.SUBMITTED, lifecycleToken: seeded.state.lifecycleToken });
    expect(await prisma.reportingStatementCommand.count({ where: { commandType: command } })).toBe(0);
    expect(await prisma.reportingStatementHistory.count({ where: { revisionId: seeded.revision.id, eventType } })).toBe(0);
    expect(await prisma.auditEvent.count({ where: { result: AuditResult.SUCCESS } })).toBe(0);
  });
  it('rejects a non-idempotent submit while the series has an unresolved SUBMITTED revision', async () => { const first = await seed(); projection.resolveInTransaction.mockResolvedValue(personalProjection(subject, submitter, year)); await expect(service.submit({ academicYearId: year, fromCivilDate: '2026-08-01', toCivilDate: '2026-08-31', requestKey: 'second-submit' }, request(submitter))).rejects.toThrow(); expect(projection.resolveInTransaction).not.toHaveBeenCalled(); expect(await prisma.reportingStatementSeries.count()).toBe(1); expect(await prisma.reportingStatementRevision.count()).toBe(1); expect(await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: first.revision.id } })).toMatchObject({ lifecycleState: State.SUBMITTED }); expect(await prisma.reportingStatementCommand.count({ where: { commandType: 'SUBMIT' } })).toBe(1); expect(await prisma.reportingStatementHistory.count({ where: { eventType: 'SUBMITTED' } })).toBe(1); });
  it('replays an accepted submit without a second projection, clock pin, or revision', async () => { projection.resolveInTransaction.mockResolvedValue(personalProjection(subject, submitter, year)); const dto = { academicYearId: year, fromCivilDate: '2026-08-01', toCivilDate: '2026-08-31', requestKey: 'exact-replay' }; const first = await service.submit(dto, request(submitter)); const replay = await service.submit(dto, request(submitter)); expect(replay).toMatchObject({ replay: true, revisionId: first.revisionId, seriesId: first.seriesId, asOfInstant: first.asOfInstant }); expect(projection.resolveInTransaction).toHaveBeenCalledTimes(1); expect(await prisma.reportingStatementRevision.count()).toBe(1); expect(await prisma.reportingStatementCommand.count({ where: { commandType: 'SUBMIT' } })).toBe(1); expect(await prisma.reportingStatementHistory.count({ where: { eventType: 'SUBMITTED' } })).toBe(1); });
  it('retains frozen submitter and history display evidence after a current profile rename', async () => { await prisma.staffProfile.create({ data: { userId: submitter, displayName: 'Tên ban đầu', staffCode: 'GV-01' } }); projection.resolveInTransaction.mockResolvedValue(personalProjection(subject, submitter, year)); const submitted = await service.submit({ academicYearId: year, fromCivilDate: '2026-08-01', toCivilDate: '2026-08-31', requestKey: 'frozen-display' }, request(submitter)); const before = await repository.readFrozenRevision(prisma, submitted.revisionId); await prisma.staffProfile.update({ where: { userId: submitter }, data: { displayName: 'Tên hiện tại', staffCode: 'GV-02' } }); const current = await prisma.staffProfile.findUniqueOrThrow({ where: { userId: submitter } }); const after = await repository.readFrozenRevision(prisma, submitted.revisionId); const history = await prisma.reportingStatementHistory.findFirstOrThrow({ where: { revisionId: submitted.revisionId, eventType: 'SUBMITTED' } }); expect(current).toMatchObject({ userId: submitter, displayName: 'Tên hiện tại', staffCode: 'GV-02' }); expect(before).toMatchObject({ submitterDisplayNameSnapshot: 'Tên ban đầu', submitterStaffCodeSnapshot: 'GV-01' }); expect(after).toMatchObject({ canonicalSnapshotJson: before?.canonicalSnapshotJson, semanticHash: before?.semanticHash, submitterDisplayNameSnapshot: 'Tên ban đầu', submitterStaffCodeSnapshot: 'GV-01' }); expect(history).toMatchObject({ actorDisplayNameSnapshot: 'Tên ban đầu', actorStaffCodeSnapshot: 'GV-01' }); });
  it('allows only one real concurrent genuinely-new submit into a logical series', async () => { projection.resolveInTransaction.mockResolvedValue(personalProjection(subject, submitter, year)); const submit = (requestKey: string) => service.submit({ academicYearId: year, fromCivilDate: '2026-08-01', toCivilDate: '2026-08-31', requestKey }, request(submitter)); const outcomes = await Promise.allSettled([submit('concurrent-submit-a'), submit('concurrent-submit-b')]); expect(outcomes.filter(x => x.status === 'fulfilled')).toHaveLength(1); expect(outcomes.filter(x => x.status === 'rejected')).toHaveLength(1); expect(await prisma.reportingStatementSeries.count()).toBe(1); expect(await prisma.reportingStatementRevision.count()).toBe(1); expect(await prisma.reportingStatementRevisionState.count({ where: { lifecycleState: State.SUBMITTED } })).toBe(1); expect(await prisma.reportingStatementCommand.count({ where: { commandType: 'SUBMIT' } })).toBe(1); expect(await prisma.reportingStatementHistory.count({ where: { eventType: 'SUBMITTED' } })).toBe(1); expect(await prisma.auditEvent.count({ where: { action: 'REPORTING_STATEMENT_SUBMITTED', result: AuditResult.SUCCESS } })).toBe(1); });
  it('retains an approved predecessor and accepts only one real concurrent successor submit', async () => { const predecessor = await seed(); await service.decide(predecessor.revision.id, { expectedLifecycleToken: predecessor.state.lifecycleToken, requestKey: 'approve-predecessor' }, request(approver), 'APPROVE'); projection.resolveInTransaction.mockResolvedValue(personalProjection(subject, submitter, year)); const submit = (requestKey: string) => service.submit({ academicYearId: year, fromCivilDate: '2026-08-01', toCivilDate: '2026-08-31', requestKey }, request(submitter)); const outcomes = await Promise.allSettled([submit('successor-submit-a'), submit('successor-submit-b')]); expect(outcomes.filter(x => x.status === 'fulfilled')).toHaveLength(1); expect(outcomes.filter(x => x.status === 'rejected')).toHaveLength(1); const revisions = await prisma.reportingStatementRevision.findMany({ where: { seriesId: predecessor.series.id }, include: { state: true }, orderBy: { submittedAt: 'asc' } }); expect(revisions).toHaveLength(2); expect(revisions[0]).toMatchObject({ id: predecessor.revision.id, state: { lifecycleState: State.APPROVED } }); expect(revisions[1]).toMatchObject({ predecessorRevisionId: predecessor.revision.id, supersedesRevisionId: predecessor.revision.id, state: { lifecycleState: State.SUBMITTED } }); expect(await prisma.reportingStatementCommand.count({ where: { commandType: 'SUBMIT' } })).toBe(2); expect(await prisma.reportingStatementHistory.count({ where: { eventType: 'SUBMITTED' } })).toBe(2); expect(await prisma.reportingStatementRevisionState.count({ where: { lifecycleState: State.SUBMITTED } })).toBe(1); expect(await prisma.reportingStatementRevisionState.count({ where: { lifecycleState: State.APPROVED } })).toBe(1); });
  it('creates a readable immutable successor after a rejected predecessor', async () => { const rejected = await seed(); await service.decide(rejected.revision.id, { expectedLifecycleToken: rejected.state.lifecycleToken, requestKey: 'reject-predecessor' }, request(approver), 'REJECT'); projection.resolveInTransaction.mockResolvedValue(personalProjection(subject, submitter, year)); const successor = await service.submit({ academicYearId: year, fromCivilDate: '2026-08-01', toCivilDate: '2026-08-31', requestKey: 'resubmit-after-rejection' }, request(submitter)); const old = await repository.readFrozenRevision(prisma, rejected.revision.id); const next = await repository.readFrozenRevision(prisma, successor.revisionId); expect(old).toMatchObject({ state: { lifecycleState: State.REJECTED } }); expect(next).toMatchObject({ seriesId: rejected.series.id, predecessorRevisionId: rejected.revision.id, supersedesRevisionId: null, state: { lifecycleState: State.SUBMITTED } }); });
  it('keeps a frozen revision byte-stable after a real teaching-assignment drift', async () => { const schoolClass = await prisma.schoolClass.create({ data: { academicYearId: year, code: `C${crypto.randomUUID().slice(0, 6)}`, name: 'Class', gradeLevel: 10 } }); const assignment = await prisma.teachingAssignment.create({ data: { academicYearId: year, schoolClassId: schoolClass.id, subjectId: subject, teacherUserId: submitter, validFrom: new Date('2026-08-01') } }); const projectionForAssignment = personalProjection(subject, submitter, year); projectionForAssignment.responsibilityManifest[0] = { teachingAssignmentId: assignment.id, schoolClassId: schoolClass.id, subjectId: subject, validFrom: '2026-08-01', validUntil: null }; const saved = await prisma.$transaction(tx => repository.persistSubmittedRevision(tx, { series: { statementProfile: 'PERSONAL_V1', submitterUserId: submitter, academicYearId: year, fromCivilDate: new Date('2026-08-01'), toCivilDate: new Date('2026-08-31') }, frozen: freezeReportingStatementSnapshot({ statementProfile: 'PERSONAL_V1', submitterUserId: submitter, asOfInstant: asOf, projection: projectionForAssignment as never }), lifecycleToken: crypto.randomUUID(), command: { actorUserId: submitter, requestKey: 'assignment-drift', requestFingerprint: 'assignment-drift-fingerprint' }, history: { actorUserId: submitter } })); const before = await repository.readFrozenRevision(prisma, saved.revision.id); await prisma.teachingAssignment.update({ where: { id: assignment.id }, data: { validUntil: new Date('2026-08-15') } }); const after = await repository.readFrozenRevision(prisma, saved.revision.id); expect(await prisma.teachingAssignment.findUniqueOrThrow({ where: { id: assignment.id } })).toMatchObject({ validUntil: new Date('2026-08-15') }); expect(after).toMatchObject({ canonicalSnapshotJson: before?.canonicalSnapshotJson, semanticHash: before?.semanticHash, asOfInstant: before?.asOfInstant }); expect(after?.subjects.map(item => item.subjectId)).toEqual(before?.subjects.map(item => item.subjectId)); });
  it('allows exactly one real concurrent terminal decision', async () => { const seeded = await seed(); const decide = (command: 'APPROVE' | 'REJECT', requestKey: string) => service.decide(seeded.revision.id, { expectedLifecycleToken: seeded.state.lifecycleToken, requestKey }, request(approver), command); const outcomes = await Promise.allSettled([decide('APPROVE', 'decision-approve'), decide('REJECT', 'decision-reject')]); expect(outcomes.filter(x => x.status === 'fulfilled')).toHaveLength(1); expect(outcomes.filter(x => x.status === 'rejected')).toHaveLength(1); const state = await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: seeded.revision.id } }); expect([State.APPROVED, State.REJECTED]).toContain(state.lifecycleState); expect(await prisma.reportingStatementCommand.count({ where: { commandType: { in: ['APPROVE', 'REJECT'] } } })).toBe(1); expect(await prisma.reportingStatementHistory.count({ where: { revisionId: seeded.revision.id, eventType: { in: ['APPROVED', 'REJECTED'] } } })).toBe(1); expect(await prisma.auditEvent.count({ where: { entityId: seeded.revision.id, result: AuditResult.SUCCESS, action: { in: ['REPORTING_STATEMENT_APPROVED', 'REPORTING_STATEMENT_REJECTED'] } } })).toBe(1); });
  it('atomically supersedes approved predecessor when correction is approved', async () => { const old = await seed(); await service.decide(old.revision.id, { expectedLifecycleToken: old.state.lifecycleToken, requestKey: 'approve-old' }, request(approver), 'APPROVE'); const oldApproved = await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: old.revision.id } }); const successor = await seed({ predecessorRevisionId: old.revision.id, supersedesRevisionId: old.revision.id }); await service.decide(successor.revision.id, { expectedLifecycleToken: successor.state.lifecycleToken, requestKey: 'approve-correction' }, request(approver), 'APPROVE'); const oldState = await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: old.revision.id } }); const successorState = await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: successor.revision.id } }); expect(oldState.lifecycleState).toBe(State.SUPERSEDED); expect(successorState.lifecycleState).toBe(State.APPROVED); expect(oldState.lifecycleToken).not.toBe(oldApproved.lifecycleToken); expect(successorState.lifecycleToken).not.toBe(successor.state.lifecycleToken); const command = await prisma.reportingStatementCommand.findFirstOrThrow({ where: { commandType: 'APPROVE', requestKey: 'approve-correction' } }); const histories = await prisma.reportingStatementHistory.findMany({ where: { commandId: command.id } }); expect(histories).toHaveLength(2); expect(histories.find(x => x.eventType === 'SUPERSEDED')).toMatchObject({ revisionId: old.revision.id, causedByRevisionId: successor.revision.id }); expect(histories.find(x => x.eventType === 'APPROVED')).toMatchObject({ revisionId: successor.revision.id }); expect(await prisma.auditEvent.count({ where: { action: 'REPORTING_STATEMENT_APPROVED', entityId: successor.revision.id, result: AuditResult.SUCCESS } })).toBe(1); expect(projection.resolveInTransaction).not.toHaveBeenCalled(); });
  it('rejects correction without changing approved predecessor', async () => { const old = await seed(); await service.decide(old.revision.id, { expectedLifecycleToken: old.state.lifecycleToken, requestKey: 'approve-old' }, request(approver), 'APPROVE'); const before = await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: old.revision.id } }); const successor = await seed({ predecessorRevisionId: old.revision.id, supersedesRevisionId: old.revision.id }); await service.decide(successor.revision.id, { expectedLifecycleToken: successor.state.lifecycleToken, requestKey: 'reject-correction' }, request(approver), 'REJECT'); const oldState = await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: old.revision.id } }); const successorState = await prisma.reportingStatementRevisionState.findUniqueOrThrow({ where: { revisionId: successor.revision.id } }); expect(oldState.lifecycleState).toBe(State.APPROVED); expect(oldState.lifecycleToken).toBe(before.lifecycleToken); expect(successorState.lifecycleState).toBe(State.REJECTED); expect(await prisma.auditEvent.count({ where: { action: 'REPORTING_STATEMENT_REJECTED', entityId: successor.revision.id, result: AuditResult.SUCCESS } })).toBe(1); expect(projection.resolveInTransaction).not.toHaveBeenCalled(); });
});
