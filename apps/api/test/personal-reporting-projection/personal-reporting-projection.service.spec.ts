import { PersonalReportingProjectionService } from '../../src/personal-reporting-projection/personal-reporting-projection.service';
const input={academicYearId:'year',targetUserId:'teacher',fromCivilDate:'2026-08-01' as never,toCivilDate:'2026-08-31' as never,asOfInstant:new Date('2026-08-10T00:00:00.000Z')};
function harness(rows: unknown[]=[]){const tx:any={academicYear:{findUnique:jest.fn().mockResolvedValue({id:'year'})},user:{findUnique:jest.fn().mockResolvedValue({id:'teacher'})},academicCalendarVersion:{findFirst:jest.fn().mockResolvedValue({startDate:new Date('2026-08-01'),endDate:new Date('2026-08-31')})},teachingAssignment:{findMany:jest.fn().mockResolvedValue(rows)} };const prisma:any={$transaction:jest.fn((f:any)=>f(tx))};const reporting:any={resolve:jest.fn(),resolveInTransaction:jest.fn()};const clock={now:()=>new Date('2026-08-10T00:00:00.000Z')};return{tx,reporting,prisma,service:new PersonalReportingProjectionService(prisma,reporting,clock)};}
describe('PersonalReportingProjectionService',()=>{it('R21 validates then returns A2 zero scope without reporting call',async()=>{const h=harness();const r=await h.service.resolve(input);expect(r).toMatchObject({status:'PASS',responsibilityState:'ZERO_RESPONSIBILITY',counts:{distributedElapsedCount:0},sections:[],responsibilityManifest:[]});expect(h.reporting.resolveInTransaction).not.toHaveBeenCalled();expect(h.prisma.$transaction).toHaveBeenCalledWith(expect.any(Function),{isolationLevel:'RepeatableRead'});});});