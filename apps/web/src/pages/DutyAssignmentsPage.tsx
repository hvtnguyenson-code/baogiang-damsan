import type { StaffAdditionalDutyAssignmentRecord } from '@baogiang/contracts';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useMemo, useState, type FormEvent } from 'react';
import { useAuth } from '../auth/auth-context';
import { Button } from '../components/ui/button';
import { FormField } from '../components/ui/form-field';
import { DataTable, EmptyState, MutationNotice, PageHeader, PageLoading, Pagination, QueryFailure, SelectField, StatusText, TextareaField } from '../components/ui/management';
import { hasSchoolCapability } from '../lib/capabilities';
import { formatDateTime, isActiveWindow } from '../lib/display';
import { catalogApi, dutyAssignmentsApi, dutyDefinitionsApi, toIso, toLocalInput, usersApi } from '../lib/management-api';

type Draft = { id?: string; staffProfileId: string; dutyDefinitionId: string; scopeType: 'SCHOOL_WIDE' | 'SUBJECT_GROUP'; scopeResourceId: string; validFrom: string; validUntil: string; note: string };
export function DutyAssignmentsPage() {
  const { auth } = useAuth(); const capabilities = auth?.capabilities ?? []; const schoolWide = hasSchoolCapability(capabilities, 'ADDITIONAL_DUTY_ASSIGNMENT_MANAGE'); const canUsers = hasSchoolCapability(capabilities, 'USER_MANAGE'); const canGroups = hasSchoolCapability(capabilities, 'SUBJECT_GROUP_MANAGE'); const exactGroups = capabilities.filter((c) => c.scope === 'SUBJECT_GROUP' && c.resourceId).map((c) => c.resourceId!); const queryClient = useQueryClient();
  const [page, setPage] = useState(1); const [activeAt, setActiveAt] = useState(''); const [draft, setDraft] = useState<Draft | null>(null); const [endId, setEndId] = useState<string>(); const [endAt, setEndAt] = useState(''); const [success, setSuccess] = useState('');
  const query = useQuery({ queryKey: ['duty-assignments', { page, activeAt }], queryFn: () => dutyAssignmentsApi.list({ page, pageSize: 20, activeAt: toIso(activeAt) }) }); const options = useQuery({ queryKey: ['duty-definitions', 'options'], queryFn: () => dutyDefinitionsApi.options({ page: 1, pageSize: 100 }) }); const users = useQuery({ queryKey: ['users', 'duty-lookup'], queryFn: () => usersApi.list({ page: 1, pageSize: 100 }), enabled: canUsers }); const groups = useQuery({ queryKey: ['catalog', 'groups', 'duty-lookup'], queryFn: () => catalogApi('subject-groups').list({ page: 1, pageSize: 100, status: 'ACTIVE' }), enabled: canGroups });
  const staffNames = useMemo(() => new Map(users.data?.items.filter((u) => u.profile).map((u) => [u.profile!.id, `${u.profile!.displayName}${u.profile!.staffCode ? ` — ${u.profile!.staffCode}` : ''}`])), [users.data]); const dutyNames = useMemo(() => new Map(options.data?.items.map((d) => [d.id, `${d.code} — ${d.name}`])), [options.data]); const groupNames = useMemo(() => new Map(groups.data?.items.map((g) => [g.id, `${g.code} — ${g.name}`])), [groups.data]);
  const permittedGroups = canGroups ? groups.data?.items ?? [] : [];
  const save = useMutation({ mutationFn: (value: Draft) => value.id ? dutyAssignmentsApi.update(value.id, { validFrom: toIso(value.validFrom), validUntil: toIso(value.validUntil), note: value.note || undefined }) : dutyAssignmentsApi.create({ staffProfileId: value.staffProfileId, dutyDefinitionId: value.dutyDefinitionId, scopeType: value.scopeType, ...(value.scopeType === 'SUBJECT_GROUP' ? { scopeResourceId: value.scopeResourceId } : {}), validFrom: toIso(value.validFrom), validUntil: toIso(value.validUntil), note: value.note || undefined }), onSuccess: async () => { setDraft(null); setSuccess('Đã lưu phân công kiêm nhiệm.'); await queryClient.invalidateQueries({ queryKey: ['duty-assignments'] }); } });
  const end = useMutation({ mutationFn: () => dutyAssignmentsApi.end(endId!, toIso(endAt)), onSuccess: async () => { setEndId(undefined); setEndAt(''); setSuccess('Đã kết thúc phân công; lịch sử vẫn được giữ.'); await queryClient.invalidateQueries({ queryKey: ['duty-assignments'] }); } });
  const canCreate = canUsers && (schoolWide || (exactGroups.length > 0 && canGroups));
  function edit(row: StaffAdditionalDutyAssignmentRecord) { setDraft({ id: row.id, staffProfileId: row.staffProfileId, dutyDefinitionId: row.dutyDefinitionId, scopeType: row.scopeType === 'SUBJECT_GROUP' ? 'SUBJECT_GROUP' : 'SCHOOL_WIDE', scopeResourceId: row.scopeResourceId ?? '', validFrom: toLocalInput(row.validFrom), validUntil: toLocalInput(row.validUntil), note: row.note ?? '' }); }
  function submit(event: FormEvent) { event.preventDefault(); if (!draft || (!draft.id && (!draft.staffProfileId || !draft.dutyDefinitionId)) || (draft.scopeType === 'SUBJECT_GROUP' && !draft.scopeResourceId)) return; save.mutate(draft); }
  return <div className="management-page duty-ledger"><PageHeader eyebrow="Phân công kiêm nhiệm" title="Kiêm nhiệm nhân sự" action={<Button type="button" disabled={!canCreate} onClick={() => setDraft({ staffProfileId: '', dutyDefinitionId: '', scopeType: schoolWide ? 'SCHOOL_WIDE' : 'SUBJECT_GROUP', scopeResourceId: exactGroups[0] ?? '', validFrom: '', validUntil: '', note: '' })}>Tạo phân công</Button>}>Danh sách được máy chủ giới hạn theo đúng phạm vi được giao. Kiêm nhiệm không tự cấp quyền hệ thống.</PageHeader>{!canCreate && <div className="limitation-note"><strong>Chưa thể tạo phân công mới.</strong> Cần quyền tra cứu nhân sự và danh mục tổ phù hợp; giao diện không yêu cầu nhập UUID thô.</div>}<MutationNotice error={save.error ?? end.error} success={success} />{draft && <form className="long-form" onSubmit={submit}><h2>{draft.id ? 'Điều chỉnh phân công' : 'Tạo phân công kiêm nhiệm'}</h2><div className="form-grid">{draft.id ? <p><strong>Nhân sự:</strong> {staffNames.get(draft.staffProfileId) ?? 'Không tra cứu được tên'}</p> : <SelectField label="Nhân sự" id="duty-staff" value={draft.staffProfileId} onChange={(e) => setDraft({ ...draft, staffProfileId: e.target.value })} required><option value="">Chọn nhân sự</option>{users.data?.items.filter((u) => u.profile).map((u) => <option key={u.profile!.id} value={u.profile!.id}>{u.profile!.displayName} — {u.profile!.staffCode ?? u.username}</option>)}</SelectField>}{draft.id ? <p><strong>Loại kiêm nhiệm:</strong> {dutyNames.get(draft.dutyDefinitionId) ?? 'Loại lịch sử'}</p> : <SelectField label="Loại kiêm nhiệm" id="duty-definition" value={draft.dutyDefinitionId} onChange={(e) => setDraft({ ...draft, dutyDefinitionId: e.target.value })} required><option value="">Chọn loại</option>{options.data?.items.map((d) => <option key={d.id} value={d.id}>{d.code} — {d.name}</option>)}</SelectField>}{!draft.id && <SelectField label="Phạm vi" id="duty-scope" value={draft.scopeType} onChange={(e) => setDraft({ ...draft, scopeType: e.target.value as Draft['scopeType'], scopeResourceId: '' })}><option value="SCHOOL_WIDE" disabled={!schoolWide}>Toàn trường</option><option value="SUBJECT_GROUP">Tổ chuyên môn</option></SelectField>}{draft.scopeType === 'SUBJECT_GROUP' && (draft.id ? <p><strong>Tổ:</strong> {groupNames.get(draft.scopeResourceId) ?? <span className="technical-value">{draft.scopeResourceId}</span>}</p> : <SelectField label="Tổ chuyên môn" id="duty-group" value={draft.scopeResourceId} onChange={(e) => setDraft({ ...draft, scopeResourceId: e.target.value })} required><option value="">Chọn tổ</option>{permittedGroups.filter((g) => schoolWide || exactGroups.includes(g.id)).map((g) => <option key={g.id} value={g.id}>{g.code} — {g.name}</option>)}</SelectField>)}<FormField label="Có hiệu lực từ" name="duty-assignment-from" type="datetime-local" value={draft.validFrom} onChange={(e) => setDraft({ ...draft, validFrom: e.target.value })} /><FormField label="Có hiệu lực đến" name="duty-assignment-until" type="datetime-local" value={draft.validUntil} onChange={(e) => setDraft({ ...draft, validUntil: e.target.value })} /></div><TextareaField label="Ghi chú" id="duty-assignment-note" rows={3} value={draft.note} onChange={(e) => setDraft({ ...draft, note: e.target.value })} /><div className="form-actions"><Button type="submit" loading={save.isPending}>Lưu phân công</Button><Button type="button" variant="quiet" onClick={() => setDraft(null)}>Hủy</Button></div></form>}<div className="filter-bar"><FormField label="Đang hiệu lực tại" name="duty-assignment-active" type="datetime-local" value={activeAt} onChange={(e) => { setActiveAt(e.target.value); setPage(1); }} /></div>{query.isPending ? <PageLoading /> : query.isError ? <QueryFailure error={query.error} retry={() => void query.refetch()} /> : query.data.items.length === 0 ? <EmptyState filtered={Boolean(activeAt)} title={activeAt ? "Không có kiêm nhiệm trong thời điểm này" : "Chưa có phân công kiêm nhiệm"} message="Các phân công đã kết thúc vẫn được giữ trong sổ lịch sử." /> : <><DataTable label="Danh sách phân công kiêm nhiệm" headings={['Nhân sự', 'Kiêm nhiệm', 'Phạm vi', 'Hiệu lực', 'Ghi chú', 'Trạng thái', 'Thao tác']}>{query.data.items.map((row) => { const active = isActiveWindow(row.validFrom, row.validUntil); return <tr key={row.id}><td>{staffNames.get(row.staffProfileId) ?? <><span>Không tra cứu được tên</span><small className="technical-value table-secondary">{row.staffProfileId}</small></>}</td><td>{dutyNames.get(row.dutyDefinitionId) ?? <small className="technical-value">{row.dutyDefinitionId}</small>}</td><td>{row.scopeType === 'SCHOOL_WIDE' ? 'Toàn trường' : groupNames.get(row.scopeResourceId ?? '') ?? 'Tổ được giao'}</td><td>{formatDateTime(row.validFrom)}<small className="table-secondary">đến {formatDateTime(row.validUntil)}</small></td><td>{row.note ?? '—'}</td><td><StatusText active={active} /></td><td><div className="row-actions"><Button type="button" variant="quiet" onClick={() => edit(row)}>Sửa hiệu lực</Button>{active && (endId === row.id ? <><FormField label="Kết thúc lúc" name={`duty-end-${row.id}`} type="datetime-local" value={endAt} onChange={(e) => setEndAt(e.target.value)} /><Button type="button" variant="secondary" loading={end.isPending} onClick={() => end.mutate()}>Xác nhận kết thúc</Button><Button type="button" variant="quiet" onClick={() => setEndId(undefined)}>Hủy</Button></> : <Button type="button" variant="quiet" onClick={() => setEndId(row.id)}>Kết thúc</Button>)}</div></td></tr>; })}</DataTable><Pagination page={query.data.page} pageSize={query.data.pageSize} total={query.data.total} onPage={setPage} /></>}</div>;
}
