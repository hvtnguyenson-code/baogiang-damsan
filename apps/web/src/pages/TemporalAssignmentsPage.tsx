import type { StaffSubjectRecord, SubjectGroupMembershipRecord, UserManagementRecord } from '@baogiang/contracts';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useMemo, useState, type FormEvent } from 'react';
import { useAuth } from '../auth/auth-context';
import { Button } from '../components/ui/button';
import { FormField } from '../components/ui/form-field';
import { DataTable, EmptyState, MutationNotice, PageHeader, PageLoading, Pagination, QueryFailure, SelectField, StatusText } from '../components/ui/management';
import { hasSchoolCapability } from '../lib/capabilities';
import { formatDateTime, isActiveWindow } from '../lib/display';
import { assignmentApi, catalogApi, toIso, toLocalInput, usersApi } from '../lib/management-api';

type RecordType = SubjectGroupMembershipRecord | StaffSubjectRecord;
type Draft = { id?: string; userId: string; resourceId: string; validFrom: string; validUntil: string; isPrimary: boolean };

export function TemporalAssignmentsPage({ kind }: { kind: 'subject-group-memberships' | 'staff-subjects' }) {
  const isGroup = kind === 'subject-group-memberships'; const title = isGroup ? 'Phân công tổ chuyên môn' : 'Phân công môn học'; const resourceLabel = isGroup ? 'Tổ chuyên môn' : 'Môn học';
  const { auth } = useAuth(); const canLookupUsers = hasSchoolCapability(auth?.capabilities ?? [], 'USER_MANAGE'); const queryClient = useQueryClient(); const [page, setPage] = useState(1); const [activeAt, setActiveAt] = useState(''); const [draft, setDraft] = useState<Draft | null>(null); const [endId, setEndId] = useState<string>(); const [endAt, setEndAt] = useState(''); const [success, setSuccess] = useState('');
  const api = assignmentApi(kind); const resourceApi = catalogApi(isGroup ? 'subject-groups' : 'subjects'); const queryKey = ['assignments', kind, { page, activeAt }] as const;
  const query = useQuery({ queryKey, queryFn: () => api.list({ page, pageSize: 20, activeAt: toIso(activeAt) }) });
  const users = useQuery({ queryKey: ['users', 'lookup'], queryFn: () => usersApi.list({ page: 1, pageSize: 100 }), enabled: canLookupUsers });
  const resources = useQuery({ queryKey: ['catalog', isGroup ? 'subject-groups' : 'subjects', 'lookup'], queryFn: () => resourceApi.list({ page: 1, pageSize: 100, status: 'ACTIVE' }) });
  const save = useMutation({ mutationFn: (value: Draft) => value.id ? api.update(value.id, { validFrom: toIso(value.validFrom), validUntil: toIso(value.validUntil), isPrimary: value.isPrimary }) : api.create({ userId: value.userId, resourceId: value.resourceId, validFrom: toIso(value.validFrom), validUntil: toIso(value.validUntil), isPrimary: value.isPrimary }), onSuccess: async () => { setDraft(null); setSuccess('Đã lưu phân công.'); await queryClient.invalidateQueries({ queryKey: ['assignments', kind] }); } });
  const endMutation = useMutation({ mutationFn: () => api.end(endId!, toIso(endAt)), onSuccess: async () => { setEndId(undefined); setEndAt(''); setSuccess('Đã kết thúc phân công; lịch sử vẫn được giữ lại.'); await queryClient.invalidateQueries({ queryKey: ['assignments', kind] }); } });
  const userNames = useMemo(() => new Map(users.data?.items.map((u) => [u.id, displayUser(u)])), [users.data]); const resourceNames = useMemo(() => new Map(resources.data?.items.map((r) => [r.id, `${r.code} — ${r.name}`])), [resources.data]);
  function submit(event: FormEvent) { event.preventDefault(); if (draft && draft.userId && draft.resourceId) save.mutate(draft); }
  function edit(row: RecordType) { setDraft({ id: row.id, userId: row.userId, resourceId: isGroup ? (row as SubjectGroupMembershipRecord).subjectGroupId : (row as StaffSubjectRecord).subjectId, validFrom: toLocalInput(row.validFrom), validUntil: toLocalInput(row.validUntil), isPrimary: row.isPrimary }); }
  return <div className="management-page temporal-ledger"><PageHeader eyebrow="Phân công có hiệu lực" title={title} action={<Button type="button" disabled={!canLookupUsers} onClick={() => setDraft({ userId: '', resourceId: '', validFrom: '', validUntil: '', isPrimary: false })}>Tạo phân công</Button>}>Khoảng hiệu lực dùng quy tắc từ thời điểm bắt đầu đến trước thời điểm kết thúc.</PageHeader>{!canLookupUsers && <div className="limitation-note"><strong>Chưa thể tạo phân công.</strong> Tài khoản này không có quyền đọc danh sách người dùng; hệ thống không yêu cầu nhập mã kỹ thuật thay cho việc chọn người.</div>}<MutationNotice error={save.error ?? endMutation.error} success={success} />{draft && <form className="inline-work-form" onSubmit={submit}><h2>{draft.id ? 'Điều chỉnh hiệu lực' : 'Tạo phân công'}</h2><div className="form-grid">{draft.id ? <p><strong>Người:</strong> {userNames.get(draft.userId) ?? 'Mã kỹ thuật không tra cứu được'}</p> : <SelectField label="Người được phân công" id={`${kind}-user`} value={draft.userId} onChange={(e) => setDraft({ ...draft, userId: e.target.value })} required><option value="">Chọn người</option>{users.data?.items.map((u) => <option key={u.id} value={u.id}>{displayUser(u)}</option>)}</SelectField>}{draft.id ? <p><strong>{resourceLabel}:</strong> {resourceNames.get(draft.resourceId) ?? 'Mã kỹ thuật không tra cứu được'}</p> : <SelectField label={resourceLabel} id={`${kind}-resource`} value={draft.resourceId} onChange={(e) => setDraft({ ...draft, resourceId: e.target.value })} required><option value="">Chọn {resourceLabel.toLowerCase()}</option>{resources.data?.items.map((r) => <option key={r.id} value={r.id}>{r.code} — {r.name}</option>)}</SelectField>}<FormField label="Có hiệu lực từ" name={`${kind}-from`} type="datetime-local" value={draft.validFrom} onChange={(e) => setDraft({ ...draft, validFrom: e.target.value })} hint="Để trống khi muốn máy chủ dùng thời điểm mặc định." /><FormField label="Có hiệu lực đến" name={`${kind}-until`} type="datetime-local" value={draft.validUntil} onChange={(e) => setDraft({ ...draft, validUntil: e.target.value })} hint="Thời điểm này không còn thuộc khoảng hiệu lực." /></div><label className="check-field"><input type="checkbox" checked={draft.isPrimary} onChange={(e) => setDraft({ ...draft, isPrimary: e.target.checked })} /> Phân công chính</label><div className="form-actions"><Button type="submit" loading={save.isPending}>Lưu phân công</Button><Button type="button" variant="quiet" onClick={() => setDraft(null)}>Hủy</Button></div></form>}<div className="filter-bar"><FormField label="Đang hiệu lực tại" name={`${kind}-active-at`} type="datetime-local" value={activeAt} onChange={(e) => { setActiveAt(e.target.value); setPage(1); }} /><Button type="button" variant="quiet" onClick={() => setActiveAt('')}>Xóa lọc thời gian</Button></div>{query.isPending ? <PageLoading /> : query.isError ? <QueryFailure error={query.error} retry={() => void query.refetch()} /> : query.data.items.length === 0 ? <EmptyState filtered={Boolean(activeAt)} title={activeAt ? "Không có phân công trong thời điểm này" : "Chưa có lịch sử phân công"} message={activeAt ? "Chọn thời điểm khác hoặc xóa lọc thời gian." : "Phân công mới và các lần đã kết thúc sẽ được ghi tại đây."} /> : <><DataTable label={title} headings={['Người', resourceLabel, 'Hiệu lực', 'Loại', 'Trạng thái', 'Thao tác']}>{query.data.items.map((raw) => { const row = raw as RecordType; const resourceId = isGroup ? (row as SubjectGroupMembershipRecord).subjectGroupId : (row as StaffSubjectRecord).subjectId; const active = isActiveWindow(row.validFrom, row.validUntil); return <tr key={row.id}><td>{userNames.get(row.userId) ?? <><span>Không tra cứu được tên</span><small className="technical-value table-secondary">{row.userId}</small></>}</td><td>{resourceNames.get(resourceId) ?? <small className="technical-value">{resourceId}</small>}</td><td><span className="technical-value">{formatDateTime(row.validFrom)}</span><small className="table-secondary">đến {formatDateTime(row.validUntil)}</small></td><td>{row.isPrimary ? 'Phân công chính' : 'Phân công khác'}</td><td><StatusText active={active} /></td><td><div className="row-actions"><Button type="button" variant="quiet" onClick={() => edit(row)}>Sửa hiệu lực</Button>{active && (endId === row.id ? <><FormField label="Kết thúc lúc" name={`end-${row.id}`} type="datetime-local" value={endAt} onChange={(e) => setEndAt(e.target.value)} /><Button type="button" variant="secondary" loading={endMutation.isPending} onClick={() => endMutation.mutate()}>Xác nhận kết thúc</Button><Button type="button" variant="quiet" onClick={() => setEndId(undefined)}>Hủy</Button></> : <Button type="button" variant="quiet" onClick={() => setEndId(row.id)}>Kết thúc</Button>)}</div></td></tr>; })}</DataTable><Pagination page={query.data.page} pageSize={query.data.pageSize} total={query.data.total} onPage={setPage} /></>}</div>;
}

function displayUser(user: UserManagementRecord) { return `${user.profile?.displayName ?? user.username}${user.profile?.staffCode ? ` — ${user.profile.staffCode}` : ''}`; }
