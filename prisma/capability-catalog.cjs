const VALID_SCOPE_TYPES = new Set(['PERSONAL', 'SUBJECT', 'SUBJECT_GROUP', 'SCHOOL_WIDE', 'ACTIVITY']);

const CAPABILITIES = [
  ['HOMEROOM_ASSIGNMENT_MANAGE', 'Quản lý phân công giáo viên chủ nhiệm toàn trường.', ['SCHOOL_WIDE']],
  ['TEACHER_BASE', 'Nền tảng truy cập chuyên môn cá nhân.', ['PERSONAL']], ['SUBJECT_GROUP_LEAD', 'Điều phối tổ chuyên môn trong phạm vi được cấp.', ['SUBJECT_GROUP']], ['APPROVAL_PRINCIPAL', 'Phê duyệt chuyên môn cấp trường dành cho người được cấp.', ['SCHOOL_WIDE']], ['APPROVAL_VICE_PRINCIPAL', 'Phê duyệt chuyên môn cấp trường theo phân công.', ['SCHOOL_WIDE']], ['GDDDP_COORDINATOR', 'Điều phối Giáo dục địa phương.', ['ACTIVITY']], ['HĐTN_COORDINATOR', 'Điều phối Hoạt động trải nghiệm, hướng nghiệp.', ['ACTIVITY']], ['SYSTEM_ADMIN', 'Quản trị kỹ thuật; không tự cấp quyền chuyên môn.', ['SCHOOL_WIDE']], ['USER_MANAGE', 'Quản trị tài khoản và trạng thái người dùng.', ['SCHOOL_WIDE']], ['SUBJECT_GROUP_MANAGE', 'Quản trị danh mục tổ chuyên môn và membership.', ['SCHOOL_WIDE']], ['SUBJECT_MANAGE', 'Quản trị danh mục môn học và phân công giảng dạy.', ['SCHOOL_WIDE']], ['CAPABILITY_GRANT', 'Cấp và thu hồi capability theo scope.', ['SCHOOL_WIDE']], ['AUDIT_VIEW', 'Xem audit log theo phạm vi được cấp.', ['SCHOOL_WIDE']], ['ADDITIONAL_DUTY_CATALOG_MANAGE', 'Quản trị catalog loại kiêm nhiệm.', ['SCHOOL_WIDE']], ['ACADEMIC_STRUCTURE_MANAGE', 'Quản trị năm học, phiên lịch học thuật và lớp học.', ['SCHOOL_WIDE']], ['TIMETABLE_MANAGE', 'Quản trị khung tiết và thời khóa biểu toàn trường.', ['SCHOOL_WIDE']], ['PPCT_MANAGE', 'Quản trị PPCT theo môn hoặc toàn trường.', ['SUBJECT', 'SCHOOL_WIDE']], ['ADDITIONAL_DUTY_ASSIGNMENT_MANAGE', 'Quản trị phân công kiêm nhiệm theo scope và hiệu lực.', ['SUBJECT_GROUP', 'SCHOOL_WIDE']], ['AI_ACTIVE_USE_SCHOOL', 'Sử dụng AI chủ động cấp trường khi policy cho phép.', ['SCHOOL_WIDE']], ['AI_ACTIVE_USE_DEPARTMENT', 'Sử dụng AI chủ động trong tổ chuyên môn khi policy cho phép.', ['SUBJECT_GROUP']], ['AI_ACTIVE_USE_ACTIVITY', 'Sử dụng AI chủ động cho hoạt động được cấp.', ['ACTIVITY']], ['AI_RECEIVE_SUGGESTION', 'Nhận gợi ý AI thụ động.', ['PERSONAL']], ['AI_CONFIRM_SUGGESTION', 'Xác nhận gợi ý AI trước business command.', ['PERSONAL']], ['AI_EDIT_DRAFT', 'Chỉnh sửa bản nháp do AI tạo.', ['PERSONAL']], ['AI_CONFIGURE_POLICY', 'Quản trị policy, task, quota và budget AI.', ['SCHOOL_WIDE']], ['AI_VIEW_USAGE', 'Xem số liệu sử dụng AI.', ['SCHOOL_WIDE']], ['AI_VIEW_COST', 'Xem chi phí AI.', ['SCHOOL_WIDE']], ['AI_VIEW_AUDIT', 'Xem audit AI.', ['SCHOOL_WIDE']], ['AI_DISABLE_SYSTEM', 'Tắt AI toàn hệ thống theo policy.', ['SCHOOL_WIDE']], ['CALENDAR_EXCEPTION_MANAGE', 'Quản lý ngoại lệ lịch học toàn trường.', ['SCHOOL_WIDE']], ['TEACHING_OPERATION_MANAGE', 'Quản lý điều hành tiết dạy theo môn hoặc toàn trường.', ['SUBJECT', 'SCHOOL_WIDE']], ['SPECIAL_ACTIVITY_MANAGE', 'Special Activity management school-wide.', ['SCHOOL_WIDE']], ['TEACHING_EXECUTION_RECORD', 'Ghi nhận và hiệu chỉnh bằng chứng thực hiện giảng dạy cá nhân.', ['PERSONAL']], ['TEACHING_EXECUTION_MANAGE', 'Quản lý bằng chứng thực hiện giảng dạy theo môn hoặc toàn trường.', ['SUBJECT', 'SCHOOL_WIDE']], ['REPORTING_READ', 'Xem báo cáo giảng dạy theo môn hoặc toàn trường.', ['SUBJECT', 'SCHOOL_WIDE']], ['REPORTING_STATEMENT_SUBMIT', 'Nộp báo cáo kê khai cá nhân.', ['PERSONAL']], ['REPORTING_STATEMENT_READ', 'Xem báo cáo kê khai đã lưu.', ['PERSONAL', 'SUBJECT', 'SCHOOL_WIDE']],
];

function validateCapabilityCatalog(catalog = CAPABILITIES) {
  const keys = new Set();
  for (const [key, description, scopes] of catalog) {
    if (typeof key !== 'string' || key.trim() === '' || key !== key.trim() || key !== key.toUpperCase() || keys.has(key)) throw new Error('Capability key must be unique normalized uppercase text.');
    keys.add(key);
    if (typeof description !== 'string' || description.trim() === '') throw new Error(`Capability description is empty: ${key}`);
    if (!Array.isArray(scopes) || scopes.length === 0 || new Set(scopes).size !== scopes.length || scopes.some((scope) => !VALID_SCOPE_TYPES.has(scope))) throw new Error(`Capability scopes are invalid: ${key}`);
  }
  return true;
}
const dataFor = ([key, description, allowedScopeTypes]) => ({ key, description, allowedScopeTypes, isSystem: true, isActive: true });
async function syncCapabilityCatalog(prisma, catalog = CAPABILITIES) { validateCapabilityCatalog(catalog); await prisma.$transaction(catalog.map((entry) => { const data = dataFor(entry); return prisma.capabilityDefinition.upsert({ where: { key: data.key }, update: data, create: data }); })); return { expectedDefinitionCount: catalog.length }; }
async function verifyCapabilityCatalog(prisma, catalog = CAPABILITIES) { validateCapabilityCatalog(catalog); const expected = new Map(catalog.map((entry) => [entry[0], dataFor(entry)])); const rows = await prisma.capabilityDefinition.findMany({ where: { key: { in: [...expected.keys()] } }, select: { key: true, description: true, allowedScopeTypes: true, isSystem: true, isActive: true } }); if (rows.length !== expected.size || new Set(rows.map((row) => row.key)).size !== expected.size) throw new Error('Capability catalog verification did not return every expected key exactly once.'); for (const row of rows) { const wanted = expected.get(row.key); if (!wanted || row.description !== wanted.description || row.isSystem !== wanted.isSystem || row.isActive !== wanted.isActive || row.allowedScopeTypes.length !== wanted.allowedScopeTypes.length || row.allowedScopeTypes.some((scope, index) => scope !== wanted.allowedScopeTypes[index])) throw new Error(`Capability catalog verification mismatch: ${row.key}`); } return { expectedDefinitionCount: expected.size, verifiedDefinitionCount: rows.length }; }
module.exports = { CAPABILITIES, VALID_SCOPE_TYPES, validateCapabilityCatalog, syncCapabilityCatalog, verifyCapabilityCatalog };
